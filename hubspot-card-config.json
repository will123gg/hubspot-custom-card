{
  "name": "External URL Viewer Card",
  "description": "A custom card that displays deal information and opens an external URL",
  "sourceUrl": "${REPL_SLUG}.replit.app",
  "cardTypes": ["DEAL"],
  "configureUrl": "${REPL_SLUG}.replit.app/configure",
  "cardConfigs": [
    {
      "name": "urlPropertyName",
      "label": "URL Property",
      "description": "Select the deal property that contains the external URL",
      "type": "dealProperty",
      "propertyTypes": ["string"],
      "required": true
    }
  ],
  "appId": "9520370",
  "ci": {
    "type": "github",
    "baseUrl": "https://api.github.com",
    "repoName": "hubspot-custom-card"
  }
}